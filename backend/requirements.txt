# 随身理财公司 · 后端依赖（重构后：轻量、无重型编排框架）
fastapi>=0.115
uvicorn[standard]>=0.32
aiosqlite>=0.22          # 异步 SQLite，不阻塞事件循环
httpx>=0.28              # 行情 + 模型调用（OpenAI 兼容）
python-dotenv>=1.0
apscheduler>=3.10,<4     # 定时晨报（替代自研 asyncio 循环）

# 测试 / 静态检查（开发）
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.8
