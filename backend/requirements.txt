# 后端依赖（MVP）
# 编排层
langgraph>=1.2
langgraph-checkpoint-sqlite>=3.1
aiosqlite>=0.22
langchain-core>=0.3
# 模型接入（OpenAI 兼容端点）
langchain-openai>=0.3
# Web
fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
# 行情源（东方财富批量行情；stdlib urllib 不够稳，httpx 已在 venv）
httpx>=0.28
# MCP server（demo_data 7 tool 外置；mcp.server.mcpserver 需 2.2+）
mcp>=2.2
# 可选：行情源 yfinance 模式（Settings 可选，默认 auto 可降级快照）
# yfinance>=1.0
# 测试 / 静态检查（开发）
pytest>=8.0
ruff>=0.8
